{
  "type": "object",
  "required": ["winner", "scores", "reason", "decisionRequest"],
  "additionalProperties": false,
  "properties": {
    "winner": { "type": "string" },
    "scores": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["candidate", "score", "reason"],
        "additionalProperties": false,
        "properties": {
          "candidate": { "type": "string" },
          "score": { "type": "integer" },
          "reason": { "type": "string" }
        }
      }
    },
    "reason": { "type": "string" },
    "decisionRequest": {{ includeTemplate "agent-defs/schemas/_decision-request.json" . }}
  }
}
