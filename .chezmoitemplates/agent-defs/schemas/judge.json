{
  "type": "object",
  "required": ["winner", "scores", "reason", "decisionRequestPath"],
  "additionalProperties": false,
  "properties": {
    "winner": { "type": "string" },
    "scores": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["candidate", "score", "reason"],
        "additionalProperties": false,
        "properties": {
          "candidate": { "type": "string" },
          "score": { "type": "integer" },
          "reason": { "type": "string" }
        }
      }
    },
    "reason": { "type": "string" },
    "decisionRequestPath": { "type": ["string", "null"] }
  }
}
